### Backend systems engineer

Mostly Go. I care about how the runtime works underneath — value/pointer
semantics, escape analysis, the GC — and about correctness you can enforce
at compile time rather than re-assert in prose. Going deep on Rust.

I run [**koopa0.dev**](https://koopa0.dev) — a private Go + PostgreSQL personal
OS for commitments, learning, notes, and publishing. Agents share the same
semantic state through a deliberately small MCP surface; the admin UI keeps
high-commitment decisions with me.
